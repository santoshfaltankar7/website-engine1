/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
domains: ['via.placeholder.com', 'i.ibb.co','randomuser.me','images.unsplash.com','plus.unsplash.com'],  },
};

export default nextConfig;
